//Modules - Encapsulated code(only share minimum)...every file is a module 
 
const names = require('./learnt/4-names')
const sayHi = require('./learnt/5-utiles')
const data = require('./learnt/6-alternativeFlavour')

require('./learnt/7-midgrenade')


// sayHi('susan')
// sayHi(names.john)
// sayHi(names.peter)

// node has a bunch of modules, such as 
// -OS
// -PATH
// -FS
// -HTTP